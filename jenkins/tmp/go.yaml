    ####################################################################################################
    # Install Go
    ####################################################################################################

---
# - name: Play3
#   hosts: servers
#   vars:
#     temp_dir: /tmp/go
#     file_dir: /usr/local
#     rm_dir: /usr/local/go
#     url_path: https://go.dev/dl/go1.19.2.linux-amd64.tar.gz
#   tasks:
    - name: "Delete directory - {{ rm_dir }}"
      file:
        path: "{{ rm_dir }}"
        state: absent

    - name: "Create directory temp- {{ temp_dir }}"
      file: 
        path: "{{ temp_dir }}" 
        state: directory

    - name: "Create directory - {{ file_dir }}"
      file:
        path: "{{ file_dir }}"
        state: directory

    - name: "Download - {{ url_path }}"
      get_url:
        url: "{{ url_path }}"
        dest: "{{ temp_dir }}"
      register: file_name

    - name: "Extract - {{ file_name.dest }}"
      unarchive: 
        src: "{{ file_name.dest }}"
        dest: "{{ file_dir }}"
        remote_src: yes

    - name: "Delete directory - {{ temp_dir }}"
      file:
        path: "{{ temp_dir }}"
        state: absent

    - name: "export PATH=$PATH:/usr/local/go/bin"
      shell: "export PATH=$PATH:/usr/local/go/bin"

    - name: "echo 'export PATH=$PATH:/usr/local/go/bin' >> /etc/profile"
      shell: "echo 'export PATH=$PATH:/usr/local/go/bin' >> /etc/profile"
      register: cmd
    
    - debug:
        msg: 
        - "{{ cmd.cmd }}"
        - "{{ cmd.stdout }}"  

    # - name: "Check - go version"
    #   shell: "go version"
    #   register: cmd
    
    # - debug:
    #     msg: 
    #     - "{{ cmd.cmd }}"
    #     - "{{ cmd.stdout }}"
... 



